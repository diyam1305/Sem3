#include<stdio.h>
void area(int);
void main()
{
	int r;
	printf("Enter value of r:");
	scanf("%d", &r);
	area(r);
}
void area(int r)
{
	float PI=3.14, area;
	area=PI*r*r;
	printf("Area of circle is:%f", area);
}
